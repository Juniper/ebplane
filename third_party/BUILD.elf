load("@rules_foreign_cc//tools/build_defs:configure.bzl", "configure_make")

filegroup(name = "libelf_all", srcs = glob(["**"]), visibility = ["//visibility:public"])

configure_make(
    name = "libelf",
    lib_source = ":libelf_all",
    out_lib_dir = "lib",
    shared_libraries = ['libelf.so'],
    visibility = ["//visibility:public"]
)
